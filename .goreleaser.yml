version: 2

before:
  hooks:
    - go mod tidy
    - go test ./...

builds:
  - id: reqflow
    main: ./cmd/reqflow
    binary: reqflow
    ldflags:
      - -s -w -X github.com/thzgajendra/reqflow.Version={{.Version}}
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64

archives:
  - id: reqflow
    builds:
      - reqflow
    formats:
      - tar.gz
    format_overrides:
      - goos: windows
        formats:
          - zip
    name_template: "reqflow_{{ .Version }}_{{ .Os }}_{{ .Arch }}"

checksum:
  name_template: 'checksums.txt'

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^chore:'

release:
  github:
    owner: thzgajendra
    name: reqflow
  prerelease: auto
